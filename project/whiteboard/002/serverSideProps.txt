- Server Side Rendering

  functions

        getServerSideProps()


        parameter => context

        return values
            => {
                props,
                notFound: true,
                redirect: {
                    destination: '',
                    permanent: boolean
                }
            }
